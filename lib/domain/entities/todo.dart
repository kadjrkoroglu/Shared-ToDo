class Todo {
  final int id;
  final int listId;
  final String title;
  final bool isCompleted;

  const Todo({
    required this.id,
    required this.listId,
    required this.title,
    required this.isCompleted,
  });
}
